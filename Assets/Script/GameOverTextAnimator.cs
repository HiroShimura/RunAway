using UnityEngine;

public class GameOverTextAnimator : MonoBehaviour {
    string text;
    char[] textSplitArray;
    void Awake() {
        /*
        textSplitArray = new char[8];
        text = GetComponent<Text>().text;
        for (int i = 0; i < 8; i++) {
            textSplitArray[i] = text[i];
            Debug.Log(textSplitArray[i]);
        }
        */
    }

}
